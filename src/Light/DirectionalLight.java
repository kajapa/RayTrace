package Light;

import Primitives.Collision;
import Primitives.cVector;

public class DirectionalLight {
    //cVector getDiffuse(cVector cameraPosition, Collision info);
    //cVector getSpecular(cVector cameraPosition, Collision info);
    //int isInShadow(Collision info,Shape shape);??
}
